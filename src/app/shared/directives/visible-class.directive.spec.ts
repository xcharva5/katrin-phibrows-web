import { VisibleClassDirective } from './visible-class.directive';

describe('VisibleClassDirective', () => {
  it('should create an instance', () => {
    const directive = new VisibleClassDirective();
    expect(directive).toBeTruthy();
  });
});
